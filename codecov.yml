# Disable PR bot comments — status checks are sufficient
comment: false

coverage:
  status:
    project:
      default:
        informational: false
    patch:
      default:
        informational: false